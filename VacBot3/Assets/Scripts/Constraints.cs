using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//This script will check to make sure we don't surpass the max amount of certain objects that can be placed
//1 vacuum per grid
//7 obstacles
//Player may still trap vacuum so maybe find a way to prevent this

public class Constraints : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
